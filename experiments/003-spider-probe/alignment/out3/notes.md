# Round 3 notes

## What came out

- `k1-plate.png`: wide nocturnal forest-floor establishing frame. A wolf spider sits in the left third under one low, gold-white torch source; wet moss, oak litter, rotten wood and deep black negative space are all readable.
- `k1-hud.png`: plate-matched edit with a single white scan sweep, edge calibration marks, the exact label `SURFACE RESPONSE | MOSS`, and one small warm-red alert dot.
- `k2-plate.png`: low macro stride over dew-covered moss, with sharp legs and dark room on the right.
- `k2-hud.png`: plate-matched edit with leg-tip lock brackets, a jointed pose skeleton, two real measurement callouts, a right-edge telemetry scale and trace, and one small warm-red alert square.
- `k3-plate.png`: side-on rightward crossing over a wet mossy log, with clear trail room behind the spider.
- `k3-hud.png`: plate-matched edit with five progressively fading earlier-position samples, a white frame around the present spider, edge timeline ticks, exact timecode and one warm-red alert marker.
- All six deliverables are PNG files normalized to exactly 1600 × 900.

## Rule misses / caveats

- No obvious plate contamination: the three plates contain no HUD, text, drawn lines or motion copies.
- Required HUD text is short and legible, with no visible gibberish or stray pseudo-labels.
- The HUDs were made as built-in image edits of their corresponding plates and visually retain the plate underneath. Because the edit is generative rather than a deterministic vector composite, literal pixel-for-pixel identity outside the intended overlay cannot be guaranteed.
- In `k2-hud.png`, the clearest six visible leg endpoints have full corner brackets; the two raised/rear endpoints are primarily indicated by the pose-skeleton endpoint nodes.

